# 🚀 快速部署指南

## 方式一：使用本地部署脚本（最简单）

### Windows

```cmd
:: 1. 配置 SSH（首次使用）
deploy\setup-ssh.bat

:: 2. 运行部署脚本
deploy\local-deploy.bat
```

### 说明

1. **setup-ssh.bat** 会自动：
   - 生成 SSH 密钥
   - 创建 SSH 配置文件
   - 启动 ssh-agent
   
2. **local-deploy.bat** 会自动：
   - 检查并提交更改
   - 推送到 GitHub
   - 触发 GitHub Actions 部署

---

## 方式二：手动命令

### 1. 生成 SSH 密钥

```powershell
# PowerShell
ssh-keygen -t ed25519 -C "your_email@example.com"
```

### 2. 配置 SSH

编辑 `~/.ssh/config`：

```
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519
```

### 3. 添加公钥到 GitHub

```powershell
# 查看公钥
type $env:USERPROFILE\.ssh\id_ed25519.pub

# 复制内容到 GitHub:
# https://github.com/settings/keys
```

### 4. 测试连接

```powershell
ssh -T git@github.com
```

### 5. 推送代码

```powershell
cd c:\Users\alice\qwen3.5
git push origin main
```

---

## 方式三：使用 GitHub Desktop

1. 打开 GitHub Desktop
2. 添加本地仓库 `c:\Users\alice\qwen3.5`
3. 输入提交信息
4. 点击 "Commit"
5. 点击 "Push origin"

---

## 📋 配置 GitHub Secrets

在 GitHub 仓库设置中添加以下 Secrets：

**Settings → Secrets and variables → Actions**

### 前端部署

| Secret | 说明 | 示例 |
|--------|------|------|
| `FTP_SERVER` | FTP 服务器 | `ftp.hxfund.cn` |
| `FTP_USERNAME` | FTP 用户名 | `hxfund_cn` |
| `FTP_PASSWORD` | FTP 密码 | `***` |
| `API_BASE_URL` | API 地址 | `https://api.hxfund.cn` |

### 后端部署（Docker）

| Secret | 说明 | 示例 |
|--------|------|------|
| `ACR_USERNAME` | 阿里云镜像用户名 | `aliyun` |
| `ACR_PASSWORD` | 阿里云镜像密码 | `***` |
| `ACR_NAMESPACE` | 命名空间 | `your-namespace` |
| `ECS_HOST` | ECS IP | `47.100.xx.xx` |
| `ECS_USER` | SSH 用户 | `root` |
| `ECS_SSH_KEY` | SSH 私钥 | `-----BEGIN...` |
| `API_DOMAIN` | API 域名 | `api.hxfund.cn` |

---

## ✅ 验证部署

### 查看 GitHub Actions 状态

访问：`https://github.com/YOUR_USERNAME/YOUR_REPO/actions`

### 测试前端

```bash
curl https://hxfund.cn
```

### 测试后端

```bash
curl https://api.hxfund.cn/api/health
```

---

## ⚠️ 常见问题

### Q: 推送时提示权限错误

**解决**:
```powershell
# 运行 SSH 配置脚本
deploy\setup-ssh.bat
```

### Q: GitHub Actions 部署失败

**检查**:
1. Secrets 是否正确配置
2. ECS 安全组是否开放 22 端口
3. Docker 是否已安装

### Q: 如何查看部署日志

**方法**:
1. GitHub → Actions → 选择工作流
2. 点击最近的运行
3. 查看详细日志

---

**更新日期**: 2026 年 2 月 25 日
