# 黄氏家族寻根平台 - 环境变量配置

# 服务器端口
PORT=3000

# Redis 连接地址（可选，用于会话存储）
# 未配置时使用内存存储（降级方案）
REDIS_URL=redis://localhost:6379

# CORS 允许的域名（逗号分隔）
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://hxfund.cn

# 阿里云百炼 API 配置（已迁移到 CLI 配置）
# 请运行 node qwen-code.js --init 进行配置
