# 项目目录结构说明

## 📁 目录分类

### 📂 `/scripts` - 脚本工具
存放项目构建和开发工具脚本。

| 文件 | 说明 |
|------|------|
| `build.js` | 静态资源构建脚本（CSS/JS 压缩、HTML 优化） |
| `qwen-code.js` | Qwen AI CLI 工具（支持多模型对话） |
| `qwen-code.bat` | Windows 快捷启动脚本 |

**使用方式**:
```bash
# 构建生产版本
npm run build

# 启动 AI 助手
npm run qwen "你好"

# 交互模式
npm run qwen:i
```

---

### 📂 `/tests` - 测试脚本
存放 API 和功能测试脚本。

| 文件 | 说明 |
|------|------|
| `test-api.js` | API 接口功能测试 |
| `test-auth.js` | 认证系统测试（Token、速率限制） |
| `test-chat.js` | AI 对话功能测试 |

**使用方式**:
```bash
# 运行所有测试
npm test

# 运行单个测试
node tests/test-api.js
```

---

### 📂 `/docs` - 文档
存放项目文档和说明文件。

| 文件 | 说明 |
|------|------|
| `README.md` | 项目主文档（已在根目录保留副本） |
| `DEPLOYMENT.md` | 部署指南 |
| `OPTIMIZATION_REPORT.md` | 优化报告 v1 |
| `OPTIMIZATION_REPORT_V2.md` | 优化报告 v2 |
| `QWEN_SETUP.md` | Qwen AI 配置指南 |

---

### 📂 `/public` - 前端资源
存放 Web 前端静态资源。

```
public/
├── css/
│   └── style.css          # 主样式表
├── js/
│   ├── data.js            # 数据模块（家族树、字辈数据）
│   ├── main.js            # 主逻辑（页面加载、导航）
│   ├── modules.js         # 功能模块（UI 渲染）
│   ├── script.js          # AI 客户端
│   └── error-monitor.js   # 错误监控
├── html/                   # 额外 HTML 页面
└── images/                 # 图片资源
```

---

### 📂 `/server` - 后端服务
存放 Node.js 后端服务代码。

```
server/
├── index.js                # Express 服务器主文件
├── auth.js                 # 认证模块（JWT、API Key）
├── cli-wrapper.js          # AI CLI 封装
├── session-store.js        # Redis 会话存储
└── config/
    ├── .env                # 环境变量
    └── auth.json           # 认证配置
```

---

### 📂 `根目录` - 项目根文件

| 文件 | 说明 |
|------|------|
| `index.html` | 主页面（SPA 入口） |
| `package.json` | 项目配置和依赖 |
| `.gitignore` | Git 忽略配置 |
| `LICENSE` | MIT 许可证 |
| `vercel.json` | Vercel 部署配置 |
| `netlify.toml` | Netlify 部署配置 |

---

## 📊 完整目录结构

```
qwen3.5/
├── 📂 scripts/           # 脚本工具
│   ├── build.js
│   ├── qwen-code.js
│   └── qwen-code.bat
├── 📂 tests/             # 测试脚本
│   ├── test-api.js
│   ├── test-auth.js
│   └── test-chat.js
├── 📂 docs/              # 文档
│   ├── DEPLOYMENT.md
│   ├── OPTIMIZATION_REPORT.md
│   ├── OPTIMIZATION_REPORT_V2.md
│   └── QWEN_SETUP.md
├── 📂 public/            # 前端资源
│   ├── css/
│   ├── js/
│   ├── html/
│   └── images/
├── 📂 server/            # 后端服务
│   ├── index.js
│   ├── auth.js
│   ├── cli-wrapper.js
│   ├── session-store.js
│   └── config/
├── 📂 .github/           # GitHub 配置
├── 📂 dist/              # 构建输出（自动生成）
├── index.html            # 主页面
├── README.md             # 项目说明
├── package.json          # 项目配置
├── .gitignore            # Git 忽略
└── LICENSE               # 许可证
```

---

## 🔧 npm 命令速查

```bash
# 开发
npm start              # 启动服务器
npm run dev            # 开发模式（自动重启）
npm run serve          # 静态文件服务

# 构建
npm run build          # 构建生产版本

# 测试
npm test               # 运行所有测试

# AI 工具
npm run qwen "问题"    # 单次提问
npm run qwen:i         # 交互模式
```

---

**整理日期**: 2026 年 2 月 25 日
**版本**: v3.3.0
